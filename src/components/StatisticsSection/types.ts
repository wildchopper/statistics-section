import type { StatisticsContentData } from '../Statistics/types'

export interface StatisticsSectionData {
  eyebrow: string
  title: string
  description: string
  statistics: StatisticsContentData
}
